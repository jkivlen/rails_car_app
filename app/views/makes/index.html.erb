<h1>Makes#index</h1>
<p>Find me in app/views/makes/index.html.erb</p>


<ul>
<% @makes.each do |make| %>
    <li><%= link_to(make.name, make_path(make.id)) %><li>
<% end %>
</ul>

<%= link_to('New Make', new_make_path) %>